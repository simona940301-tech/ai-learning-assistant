# 電子雞系統重構提案

## 📋 任務理解確認

根據你的要求，需要完成以下 P0 和 P1 任務：

### P0（基礎架構）
- **A. 建立事件總線**：統一所有事件處理，替換 `/interact` 和 `/event`
- **B. 確認 Cron Jobs**：確保 IQ Decay 和 Daily Reset 準確執行
- **C. 修復訊息類型**：確保所有訊息類型能存入資料庫

### P1（產品價值）
- **D. 情感狀態重設計**：完善 sick/runaway 的行為恢復任務
- **E. 添加所有缺失事件**：實現 WRONGBOOK_REVIEWED, NOTE_SAVED, STREAK 的事件觸發

---

## 🎯 方案分析

### 方案 1：統一事件總線 + 後端原子化處理（推薦）

#### 架構設計
```
前端 → /api/chick/action (統一入口)
         ↓
    Event Bus (事件總線)
         ↓
    ChickReactor (後端原子化處理)
         ↓
    更新狀態 + 生成訊息
```

#### 優點
- ✅ **單一職責**：每個模組只負責一件事
- ✅ **易於擴展**：未來新增成就/徽章系統只需監聽事件總線
- ✅ **解耦設計**：狀態更新邏輯與 API 路由分離
- ✅ **易於測試**：ChickReactor 可獨立測試
- ✅ **符合你的建議**：完全按照你提出的架構

#### 缺點
- ⚠️ **重構成本較高**：需要遷移現有代碼
- ⚠️ **需要時間**：完整實現需要 2-3 小時

#### 實現步驟
1. 創建 `/api/chick/action` 統一入口
2. 創建 `ChickReactor` 模組（後端原子化處理）
3. 遷移現有事件觸發點到新 API
4. 廢棄舊的 `/interact` 和 `/event` API
5. 修復訊息類型約束
6. 確認 cron jobs 配置

---

### 方案 2：漸進式遷移（保守方案）

#### 架構設計
```
保留 /api/chick/interact（現有）
新增 /api/chick/event（新系統）
    ↓
統一事件處理層
    ↓
逐步遷移觸發點
```

#### 優點
- ✅ **風險較低**：不破壞現有功能
- ✅ **可逐步遷移**：一次遷移一個事件類型
- ✅ **向後兼容**：舊代碼繼續運作

#### 缺點
- ⚠️ **過渡期混亂**：兩套系統並存
- ⚠️ **維護成本**：需要同時維護兩套代碼
- ⚠️ **不符合最佳實踐**：最終還是要統一

#### 實現步驟
1. 保留現有 `/api/chick/interact`
2. 完善 `/api/chick/event` 使其可用
3. 新功能使用 `/api/chick/event`
4. 逐步遷移舊功能
5. 最終廢棄 `/api/chick/interact`

---

### 方案 3：最小改動方案（快速修復）

#### 架構設計
```
保留 /api/chick/interact（作為統一入口）
擴展其功能支援所有事件類型
修復訊息類型約束
```

#### 優點
- ✅ **改動最小**：只需擴展現有 API
- ✅ **快速實現**：1 小時內完成
- ✅ **風險最低**：不改變現有架構

#### 缺點
- ⚠️ **技術債**：沒有解決根本問題
- ⚠️ **擴展性差**：未來新增功能仍需修改 API
- ⚠️ **不符合你的建議**：沒有建立事件總線

#### 實現步驟
1. 擴展 `/api/chick/interact` 支援所有事件類型
2. 修復訊息類型約束（擴展資料庫允許的類型）
3. 添加缺失的事件觸發點
4. 確認 cron jobs

---

## 🔍 當前狀態檢查

### ✅ 已確認的內容
1. **Cron Jobs 配置**：
   - `chick_decay`: 每小時執行 ✅
   - `chick_daily_reset`: 每天 UTC 20:00 執行 ✅
   - 配置檔案存在於 `supabase/functions/*/supabase.toml`

2. **訊息類型問題**：
   - 資料庫約束：`S1, S2, S3, POSITIVE` 僅 4 種
   - 代碼使用：`POKE_IDLE, POKE_BUSY, IDLE_ENCOURAGE_BATTLE, IDLE_REVIEW_MISTAKES, BATTLE_VICTORY, BATTLE_LEARNING, STREAK, MILESTONE` 等 13 種
   - **問題**：大部分訊息類型無法存入資料庫

3. **事件系統**：
   - `/api/chick/interact` - 實際使用但功能簡化
   - `/api/chick/event` - 定義完整但未使用
   - **問題**：兩套系統並存，造成混淆

---

## 💡 推薦方案：方案 1（統一事件總線）

### 為什麼選擇方案 1？
1. **符合你的建議**：完全按照你提出的架構設計
2. **長期價值**：為未來擴展打下良好基礎
3. **技術債務清理**：一次性解決根本問題
4. **最佳實踐**：符合單一職責和開閉原則

### 實現細節

#### 1. 統一事件總線 API
```typescript
// /api/chick/action/route.ts
POST /api/chick/action
{
  type: 'BATTLE_END' | 'EXPLANATION_VIEWED' | 'WRONGBOOK_REVIEWED' | ...
  payload: { ... }
}
```

#### 2. ChickReactor 模組
```typescript
// packages/server/chick/reactor.ts
export class ChickReactor {
  async processEvent(event: ChickEvent): Promise<ChickStateUpdate> {
    // 原子化處理：根據事件類型更新狀態
    // 返回狀態變更和訊息候選
  }
}
```

#### 3. 訊息類型修復
- 選項 A：擴展資料庫約束（允許所有類型）
- 選項 B：映射機制（內部使用豐富類型，存儲時映射到基礎類型）

---

## 🎮 完成後對遊戲的影響

### 正面影響
1. **用戶體驗提升**：
   - 錯題本複習、筆記保存等行為立即反映到電子雞狀態
   - 連續天數更新會觸發電子雞鼓勵訊息
   - 情感狀態恢復更有意義（通過學習行為而非僅 soothe）

2. **系統穩定性**：
   - 統一的事件處理減少 bug
   - 狀態更新邏輯集中，易於調試
   - Cron jobs 確認執行，IQ 和 Fatigue 正常運作

3. **未來擴展性**：
   - 成就系統可直接監聽事件總線
   - 徽章系統可基於相同事件
   - 無需修改電子雞核心邏輯

### 潛在風險
1. **遷移期間**：舊代碼需要逐步替換
2. **測試需求**：需要完整測試所有事件類型
3. **用戶感知**：如果遷移不當，可能出現短暫的功能中斷

---

## 📝 需要你決定的流程

### 決策點 1：選擇方案
- [ ] 方案 1：統一事件總線（推薦）
- [ ] 方案 2：漸進式遷移
- [ ] 方案 3：最小改動

### 決策點 2：訊息類型處理方式
- [ ] 選項 A：擴展資料庫約束（允許所有 13 種類型）
- [ ] 選項 B：映射機制（內部豐富類型 → 存儲基礎類型）

### 決策點 3：情感狀態恢復邏輯
- [ ] 選項 A：學習行為自動恢復（對戰/詳解/錯題本 → 恢復 normal）
- [ ] 選項 B：任務系統（完成特定任務 → 恢復 normal）
- [ ] 選項 C：時間 + 行為混合（3 天內有學習行為 → 恢復 normal）

### 決策點 4：Cron Jobs 驗證
- [ ] 是否需要添加監控/日誌來確認執行？
- [ ] 是否需要手動觸發測試？

---

## 🔄 重述理解

我對你的指令的理解：

1. **建立統一事件總線**：
   - 廢棄 `/api/chick/interact` 和 `/api/chick/event`
   - 創建 `/api/chick/action` 作為唯一入口
   - 所有用戶行為都通過這個端點，傳遞標準化的 `type` 和 `payload`

2. **後端原子化處理**：
   - 創建 `ChickReactor` 模組
   - 只負責監聽事件總線並更新電子雞狀態
   - 狀態更新邏輯與 API 路由解耦

3. **確認 Cron Jobs**：
   - 驗證 `chick_decay` 和 `chick_daily_reset` 有正確配置
   - 確保它們能準確執行

4. **修復訊息類型**：
   - 確保所有訊息類型能存入資料庫
   - 保障後續數據分析

5. **情感狀態重設計**：
   - 完善 sick/runaway 的恢復邏輯
   - 不只是 soothe，而是通過學習行為恢復

6. **添加缺失事件**：
   - 實現 WRONGBOOK_REVIEWED, NOTE_SAVED, STREAK 事件觸發
   - 將用戶學習行為立即變現為電子雞的正面狀態

**請確認以上理解是否正確，以及你選擇的方案和決策點，我將開始實現代碼。**

