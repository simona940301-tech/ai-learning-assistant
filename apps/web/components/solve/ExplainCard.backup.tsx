'use client'

/**
 * DEPRECATED: legacy ExplainCard renderer is no longer used.
 * Intentionally renders nothing to prevent legacy UI from appearing.
 */
export default function ExplainCardBackup() {
  return null
}
